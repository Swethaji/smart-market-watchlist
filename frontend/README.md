# frontend

Placeholder folder. This will be scaffolded as a React + Vite app
(`npm create vite@latest . -- --template react`) once dependency
installation is approved -- see the root README for status.
