# Copy this file to .env and fill in real values. .env is git-ignored.

DATABASE_URL=sqlite:///./watchlist.db
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Market-data provider key (Alpha Vantage / Finnhub / Twelve Data / etc.)
# Leave blank until we pick a provider -- ask Claude before adding a real key.
MARKET_DATA_API_KEY=
